<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow -->
    <item>
        <shape>
            <solid android:color="#55000000" /> <!-- Shadow color -->
            <corners android:radius="10dp" /> <!-- Adjust corner radius if needed -->
        </shape>
    </item>

    <!-- Background shape -->
    <item>
        <shape>
            <stroke
                android:width="3dp"
                android:color="@color/background_1"/>
            <solid android:color="@color/quiz_bg2" /> <!-- Background color -->
            <corners android:radius="10dp" /> <!-- Adjust corner radius if needed -->
        </shape>
    </item>
</layer-list>
